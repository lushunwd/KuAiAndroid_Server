<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jd.dao.ShareDao">
    <select id="getShareList" resultType="java.util.HashMap" parameterType="map">
      SELECT
        QVALUE.*,QUSER.USERID,QUSER.UNAME,QUSER.UHEAD,QTYPE.*
      FROM
       QVALUE ,QUSER,QTYPE
      WHERE
        QVALUE.UID=QUSER.USERID
        AND QVALUE.TID = QTYPE.TID
        AND QTYPE.TID = 1
        AND QVALUE.ISCHECKDE = 1
        order by
        QVALUE.QID
        desc limit #{0},5;
    </select>
</mapper>
