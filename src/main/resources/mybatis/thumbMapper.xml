<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jd.dao.ThumbDao">
    <insert id="up">
        update qvalue set qlike =qlike+1 where qid =#{qid};
    </insert>
    <insert id="down">
        update qvalue set qunlike =qunlike+1 where qid =#{qid};
    </insert>
</mapper>
